[project]
name = "model-training"
version = "0.1.0"
description = "Bit-exact speech spectrogram pipeline for AMD Xilinx Ultra96-V2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "librosa",
    "scipy",
    "scikit-learn",
    "matplotlib",
]
